using UnityEngine;

public class EkranBoyutuHesapla : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        EkranBoyutu.EkraniHesapla();
    }

    // Update is called once per frame
    void Update()
    {

    }
}
